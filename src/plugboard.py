"""Holds the plugboard class

This class represents the plugboard of the enigma machine
The plugboard maps letters of the alphabet to other letters, but never maps a letter to itself
After the signal passes from the plugboard it passes through the rotors, defined in rotors.py"""

class Plugboard:
    def __init__():
        pass

    def convert_char(char: str) -> str:
        """Converts a letter of the alphabet to another letter, never maps a letter to itself"""
        raise NotImplementedError()